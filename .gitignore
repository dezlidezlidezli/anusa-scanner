# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# PyInstaller build output
receiver/build/
receiver/dist/
receiver/*.spec

# Generated app-icon artifacts (regenerated by make_icon.py)
receiver/appicon.icns
receiver/appicon.iconset/
receiver/appicon_preview.png
receiver/icon_smallcheck.png

# Runtime logs
scans.csv
receiver/scans.csv

# Demo data — kept local, not tracked
demo_roster.csv

# Google secrets — never commit (global patterns so a key can't slip in from ANY location)
credentials.json
token.json
service_account.json
**/service_account.json
**/credentials.json
**/token.json
prefs.json
# Any JSON dropped in receiver/ (raw service-account key downloads land here, e.g. proj-4f3a1b2c.json)
receiver/*.json

# Distributable archives — may bundle dist/ (which contains the real service-account key)
*.zip
*.dmg
